# Flow Music – Master History

## Current Session (May 23, 2026)

### 1. Bottom Player Bar – 3-Column Grid Layout
**Files**: `index.legacy.html:53-85`, `style.css:671-709`
- Wrapped existing `.player-info`, `.player-controls`, `.player-volume` in `.player-left`, `.player-center`, `.player-right` section divs
- Changed `.player-bar` from `display:flex` to `display:grid; grid-template-columns: 1fr auto 1fr` for true left/center/right alignment
- `.player-controls` lost `flex:1` and `margin:0 auto`; now centered via grid column
- `.player-info` / `.player-volume` changed from `flex:0 0 Npx` to `width: Npx` for predictable sizing
- Responsive breakpoints (900px, 600px) updated to `grid-template-columns: auto 1fr auto`

### 2. Library List Alignment & Scrolling
**Files**: `style.css:297-305`, `style.css:546-558`
- `.album-card` changed from `text-align:center` to `text-align:left`
- `#libraryGrid` / `.library-grid-wrap` changed from `overflow:hidden` to `overflow-y:auto; overflow-x:hidden` for independent scrolling
- Song rows remain left-aligned with grid column layout

### 3. Unsynced Lyrics
**Files**: `style.css:1299-1315`
- Reduced `.po-lyric-line-unsynced` font-size from 24px to 20px, font-weight 500→400
- Set `line-height: 1.7` and reduced padding to match
- `.po-lyrics-lines.unsynced-mode` padding reduced to `5vh 0 15vh`, added `gap: 0`
- JS logic unchanged (two rendering paths: `!lyricsLines` fallback and `lyricsSource === 'unsynced'` branch)

### 4. Queue Horizontal Scroll Fix
**File**: `style.css:1439-1447`
- Added `overflow-x: hidden` to `.po-qi-list` to prevent horizontal scroll from overflowing content

### 5. Shuffle & Repeat Logic
**Files**: `app.js:333-349`, `app.js:650-670`, `app.js:846-922`, `app.js:1047-1072`
- **`shuffleAll()`** now also sets `state.shuffle = true`, syncs `poShuffle` button, and clears `playbackHistory`
- **Toggle shuffle** (`poShuffle` click): when turning ON, shuffles the queue (keeps current song at index 0), when turning OFF clears history
- **`prevSong()`**: when shuffle + history empty, restarts current song (`audio.currentTime = 0`) instead of decrementing a meaningless queue index
- **`nextSong()`**: shuffle condition changed from `queue.length > 1` to `queue.length > 0` so single-song shuffle works
- **`syncOverlay()`**: now syncs `poShuffle.classList` and `poRepeat.dataset.mode/active` when overlay opens

---

## Previous Sessions

### Early May 2026 – Flow Music Overhaul
- Complete rewrite of music player UI: sidebar, main library, player bar, player overlay
- Features: song/artist/album/folder/favorites views, queue management, synced & unsynced lyrics, liquid gradient backdrop, album art color extraction, context menus, shuffle/repeat, responsive design
- Backend: Electron main process (main.js, preload.js) for audio streaming via custom `splayer://` protocol
- IndexedDB-based library persistence
